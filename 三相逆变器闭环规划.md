> 状态说明（2026-07-30）：工程已经移除 DQ 电压电流级联双环，仅保留 DQ 开环和 DQ 单电压环。本文中的双环设计、参数和调试步骤只作为历史记录，不代表当前代码接口。

## *编码与改动范围约束*

- ***文件当前是 GBK 编码**（已有的中文注释都是 GBK 字节）。实施时**禁止用 Write 工具整体重写这个文件**（Write 默认按 UTF-8 写入，会把已有的 GBK 中文注释破坏成乱码）。必须用 **Edit 工具做局部替换**，只动 `uart_proc()` 函数体这一块，其余内容原样保留。*
- *新增的代码和注释**只使用英文 ASCII 字符**，不新增任何中文注释，从根源上避免编码冲突。*
- *尽量**最小范围修改**：只替换 `uart_proc()` 函数体（[Uart_cmp.c:54-175](file:///D:/My_Program/电赛/代码/Three_phase_Inverter/Component/Uart_cmp.c:54)）和在文件里新增的命令表/处理函数，不touch文件里其它已有的中文注释段落（如 `my_printf`、`Uart_init`、`HAL_UARTEx_RxEventCallback` 等函数保持原样不动）。*

（目前不需要）后续需要修改的问题：ADC 是软件启动、连续自由运行，并未与 PWM 中点同步采样，功率控制中可能引入开关噪声和采样相位漂移。

## 标定关系

- 直流母线：`Vdc = 60 V`
- 目标线电压：`Ull_rms = 32 V`
- 输出频率：`fout = 50 Hz`
- PWM/控制频率：沿用当前 `20 kHz`
- 目标相电压有效值：\[ U_{ph,rms}=\frac{32}{\sqrt3}=18.475\text{ V} \]

- 目标相电压峰值，也就是静止坐标系电压矢量幅值：\[ U_{ref}=U_{ph,peak} =\frac{32}{\sqrt3}\sqrt2 =26.128\text{ V} \]

- SVPWM线性区最大参考矢量幅值：\[ U_{ref,max}=\frac{V_{dc}}{\sqrt3} =34.641\text{ V} \]

- 采用“线性区上限为1”的SVPWM归一化调制度：\[ m_{svpwm} =\frac{\sqrt3 U_{ref}}{V_{dc}} =\frac{\sqrt2 U_{LL,rms}}{V_{dc}} \approx0.754 \]

## 第一轮修改

### 修改内容

1. 保留现有硬件配置

先不改 TIM8 参数：

- PWM/控制频率：20 kHz
- ARR：8399
- 中点：4200
- 三路互补输出及现有死区
- 输出频率：固定 50 Hz

只替换 [Spwm_app.c (line 322)](/D:/My_Program/电赛/代码/Three_phase_Inverter/APP/Spwm_app.c:322) 之后的查表 SPWM 计算部分。

1. 实现固定50 Hz角度发生器

每次 TIM8 中断更新：

```
theta += 2.0f * PI * 50.0f / 20000.0f;
```

即：

```
theta += 0.015707963f;
```

超过 `2π` 后回绕。先验证每400次中断正好旋转一周。

1. 建立最简单的DQ电压指令

先固定：

```
ud = 26.128f;  // 对应32 Vrms线电压
uq = 0.0f;
```

初次上电测试不能直接使用26.128 V，应先用较小值，例如：

```
ud = 3.0f;
```

1. 进行逆Park变换

```
u_alpha = ud * cosf(theta) - uq * sinf(theta);
u_beta  = ud * sinf(theta) + uq * cosf(theta);
```

由于 `uq=0`，本质上就是生成一个固定幅值、50 Hz旋转的电压矢量。

1. 逆Clarke得到三相电压

```
ua = u_alpha;
ub = -0.5f * u_alpha + 0.8660254f * u_beta;
uc = -0.5f * u_alpha - 0.8660254f * u_beta;
```

先通过调试变量或DAC确认：

- 三相幅值相同；
- 相位互差120°；
- 相序为U→V→W。

1. 加入最值注入SVPWM

```
u_max = max(ua, ub, uc);
u_min = min(ua, ub, uc);
u_zero = -0.5f * (u_max + u_min);

ua_svpwm = ua + u_zero;
ub_svpwm = ub + u_zero;
uc_svpwm = uc + u_zero;
```

然后转换成占空比：

```
duty_a = 0.5f + ua_svpwm / 60.0f;
duty_b = 0.5f + ub_svpwm / 60.0f;
duty_c = 0.5f + uc_svpwm / 60.0f;
```

1. 限幅并写入CCR

第一版建议限制在5%～95%：

```
duty = clamp(duty, 0.05f, 0.95f);

CCR1 = duty_a * 8400.0f;
CCR2 = duty_b * 8400.0f;
CCR3 = duty_c * 8400.0f;
```

不要再使用当前的：

```
Mid_Value + sine_wave[k] * 0.9f
```

1. 最后再加电压软启动

基础SVPWM验证正确后，再让 `ud` 从0缓慢增加到26.128 V：

```
0 V → 26.128 V
时间：1 s
更新频率：20 kHz
每次增加约0.001306 V
```

第一轮操作范围

第一轮只建议完成以下内容：

- 角度发生器；
- 固定小幅值 `ud=3 V、uq=0`；
- 逆Park；
- 逆Clarke；
- 最值注入SVPWM；
- 三路CCR更新。

### 第一轮调试

- Dac查看，相位差信息

```
// 逆Clarke
float ua = u_alpha;
float ub = -0.5f * u_alpha + 0.8660254f * u_beta;
float uc = -0.5f * u_alpha - 0.8660254f * u_beta;
```

- 问题：单片机运行中死机（并且当前回调里还有 ADC换算、滤波、RMS、`sqrtf`、PR、DAC 等大量运算，CPU可能长期困在中断里，让主循环、串口和 SysTick 看起来“死机”。）

1. 判断是真 HardFault，还是中断占满 CPU

在 `HardFault_Handler()`、`BusFault_Handler()`、`UsageFault_Handler()` 中分别放断点，然后运行。

- 如果进入某个 Fault：说明是非法访问、栈溢出、除零等真正异常。
- 如果没有进入 Fault，但主循环 LED、串口停止：更可能是 TIM8 中断超时或中断持续重入。
- 如果 PWM 仍正常，而串口/LED不动，也更像中断负载过重，不是 MCU 完全死机。

2. 可能是示波器的原因
3. 中断内部分运算函数耗时时间过长cosf、sinf
4. 玄学？？？
5. 应该是关闭发波时，把定时器也关了！！！

## 第二轮修改

### 修改内容

#### 第二轮目标

加入三个基础模块：

1. 完整的电压软启动/软停止。
2. 简单运行状态机。
3. 最基础的软件过流保护。

#### 修改顺序

##### *第二步：增加最小状态机*

只需要四个状态：

```
typedef enum
{
    INVERTER_STOP,
    INVERTER_RAMP_UP,
    INVERTER_RUN,
    INVERTER_FAULT
} InverterState;
```

状态关系：

```
STOP
  └─ 收到启动指令 → RAMP_UP

RAMP_UP
  └─ soft_start_ratio达到1 → RUN

RUN
  └─ 收到停止指令 → STOP
  └─ 检测到故障 → FAULT

FAULT
  └─ 立即关闭PWM
  └─ 人工清除故障后 → STOP
```

第二轮先不做自动故障恢复，防止功率级反复启停。

停止状态下：

- `ud=0`
- `uq=0`
- 三个CCR先回到中点
- 再关闭TIM8主输出

##### 第三步：加入软件过流保护

你现在每相9 Ω、32 Vrms线电压时，正常相电流约：

\[ I_{phase,rms}=2.05\text{ A} \]

正常正弦峰值约：

\[ I_{phase,peak}=2.90\text{ A} \]

初始软件过流阈值可以先设置为：

```
瞬时过流预警：3.5 A
瞬时过流关断：4.0 A左右
```

但必须结合你的电流采样标定和MOS管安全范围确定。不要把阈值直接设成2.1 A，因为瞬时电流本来就会达到约2.9 A。

为防止单个ADC噪声点误触发，可以要求连续若干次越限：

```
if (fabsf(i_phase) > current_trip)
{
    overcurrent_count++;
}
else
{
    overcurrent_count = 0;
}

if (overcurrent_count >= 3)
{
    inverter_state = INVERTER_FAULT;
}
```

20 kHz下连续3次约为150 µs。软件保护只能作为补充，不能替代硬件比较器和TIM8 Break。

三相电流检查：

```
Iu：ADC实测
Iw：ADC实测
Iv：由 -(Iu + Iw) 重构
```

保护判断取三者绝对值最大值。

### 最后工作总结

#### 目标

在第一轮"角度发生器 + dq→SVPWM"基础上，加入软启停、运行状态管理、软件过流保护三个模块（对应 `三相逆变器闭环规划.md` 第二轮的要求）。

#### 实际实现（最终版本，未采用状态机枚举，改用简单标志位）

**[Spwm_app.h](vscode-webview://1gl3c0f9jssa8s2jja8mapm380uqgtf98vvvdqjtk88810m8r5r7/index.html?id=2c41629e-2d48-4e91-9565-711635874a11&parentId=1&origin=c97a604a-71c2-47e8-8c1c-31dc21577b98&swVersion=5&extensionId=Anthropic.claude-code&platform=electron&vscode-resource-base-authority=vscode-resource.vscode-cdn.net&parentOrigin=vscode-file%3A%2F%2Fvscode-app&session=659c50f3-79e4-4748-af47-f0b94c133898)**

- 声明了 `Inverter_Start()` / `Inverter_Stop()` / `Inverter_ClearFault()` 三个控制接口。
- 最初加过 `InverterState` 枚举，后来按你的要求撤掉了，改用标志位实现，更贴近原代码风格。

**[Spwm_app.c](vscode-webview://1gl3c0f9jssa8s2jja8mapm380uqgtf98vvvdqjtk88810m8r5r7/index.html?id=2c41629e-2d48-4e91-9565-711635874a11&parentId=1&origin=c97a604a-71c2-47e8-8c1c-31dc21577b98&swVersion=5&extensionId=Anthropic.claude-code&platform=electron&vscode-resource-base-authority=vscode-resource.vscode-cdn.net&parentOrigin=vscode-file%3A%2F%2Fvscode-app&session=659c50f3-79e4-4748-af47-f0b94c133898)**

- `soft_start_ratio` 沿用原有的按圈数（`SOFT_START_CYCLES=50`）计算的软启动斜坡，`Inverter_Start()` 触发从0爬升到1；新增 `stop_requested` 标志，`Inverter_Stop()` 触发从当前值软降到0，降到0后才真正关闭PWM通道（而不是原来那种直接硬关）。
- 新增 `fault_latched` 标志：三相电流（`Three_phase_I_u/v/w`）连续3次超过 `CURRENT_TRIP_A=4.0A` 就锁存故障、立即关闭PWM；锁存后 `wave8=1` 不会重新启动，必须发 `clrfault=1` 显式清除才能重启，防止功率级反复自动重启。
- 修过一个关键bug：过流判断最初写在状态分支之外，导致停机后（没有真实电流）仍可能因采样噪声被误判进故障；后来又发现一版重构把 `if(fault_latched){...}else{...return;}` 和 `wave_enable_tim8` 判断拆开了，导致正常运行时也会被提前 `return`、SVPWM代码变成死代码、输出卡在中点电平——这个问题我已经指出并给了修复代码，具体是否已经改到文件里需要你确认。

**[Uart_cmp.c](vscode-webview://1gl3c0f9jssa8s2jja8mapm380uqgtf98vvvdqjtk88810m8r5r7/index.html?id=2c41629e-2d48-4e91-9565-711635874a11&parentId=1&origin=c97a604a-71c2-47e8-8c1c-31dc21577b98&swVersion=5&extensionId=Anthropic.claude-code&platform=electron&vscode-resource-base-authority=vscode-resource.vscode-cdn.net&parentOrigin=vscode-file%3A%2F%2Fvscode-app&session=659c50f3-79e4-4748-af47-f0b94c133898)**

- `wave8=1`/`wave8=0` 改为调用 `Inverter_Start()`/`Inverter_Stop()`。
- 新增 `clrfault=1` 命令调用 `Inverter_ClearFault()` 并回显状态。

# 第三轮修改（DQ单电压环）

## 修改内容

### 一、实施前必须先确认的物理定义

这几项必须让修改代码的 AI 写进注释，不能靠猜测。

#### 1. `vd_ref` 究竟代表什么电压

当前采样的是线电压 `Uab/Ubc`，代码重构相电压后再进行 Clarke/Park。因此稳态下的 `vd` 通常对应相电压空间矢量的峰值，不是线电压 RMS。

如果期望输出线电压有效值为：

\[ V_{LL,rms} \]

对应相电压峰值参考通常为：

\[ V_{phase,peak} = V_{LL,rms}\sqrt{\frac{2}{3}} \]

例如期望线电压有效值 32 V：

\[ V_{phase,peak} = 32\sqrt{\frac{2}{3}} \approx26.13V \]

这与当前 `DQ_UO_DEFAULT=26.0f` 基本一致。因此推荐明确规定：

```
dq_vd_ref 的单位是 V，表示相电压基波峰值
```

#### 2. 电流传感器测量位置

确认 `Ia/Ic` 测的是：逆变桥侧滤波电感电流；

### 二. 推荐控制结构

推荐结构：

```
Vd*/Vq*
   ↓
电压误差
   ↓
d/q电压PI
   ↓
逆变器dq电压修正量
   ↓
+ 基准电压前馈
   ↓
Ud_inv*/Uq_inv*
   ↓
÷ Vdc
   ↓
dq矢量限幅
   ↓
反Park
   ↓
SVPWM
```

与当前代码相比，我建议增加电压参考前馈：

\(e_{vd}=v_d^*-v_d\)

\(e_{vq}=v_q^*-v_q\)

\(\Delta u_d=PI_v(e_{vd})\)

\(\Delta u_q=PI_v(e_{vq})\)

最终指令：

\(u_{inv,d}^*=v_d^*+\Delta u_d\)

\(u_{inv,q}^*=v_q^*+\Delta u_q\)

再归一化：

\(m_d^*=\frac{u_{inv,d}^*}{V_{dc}}\)

\(m_q^*=\frac{u_{inv,q}^*}{V_{dc}}\)

### 三、实际实现记录（2026-07-26）

第三轮已按“参考前馈 + DQ 双 PI 修正 + SVPWM”接入 TIM8 的 20 kHz 控制回调：

- 用户侧参考使用线电压有效值，`vref=32` 在内部换算为 `vd_ref_target=32*sqrt(2/3)=26.128 V`。
- 从 `Uuv/Uvw` 重构三相电压，经 Clarke/Park 得到 `vd/vq`；Park 和逆 Park 共用同一组角度正余弦值。
- d/q PI 使用独立积分状态、共享 `Kp/Ki`，控制周期为 `50 us`，单轴修正量限制为 `+-3 V`，并采用条件积分抗饱和。
- 最终 DQ 电压矢量限制为 `31.18 V`，与 `60 V` 母线及 `5%~95%` 占空比限制匹配。
- 默认 `Kp=0`、`Ki=0`，因此首次烧录仍等效为电压前馈；应先发送 `vref=3` 验证 `vd/vq` 极性，再通过 `vp/vi` 逐步投入闭环。
- 新增 `vref` 和 `vstat` 命令；`vstat` 在串口查询时换算线电压反馈有效值，不在 20 kHz 中断内增加额外 `sqrtf`。
- 启动、软停止完成、过流关断和故障清除都会复位 PI；软停止完成后立即退出中断，不再继续写 CCR。

Keil ARMCC 5.06u7 全量构建结果：`0 Error(s), 0 Warning(s)`。

当前限制保持不变：ADC1/ADC2 仍为软件启动、连续自由运行，尚未与 PWM 中点同步。闭环带宽提升前必须先处理同步采样问题。

### 四、调试步骤

- 采样值做Clarke变换、Park变换是否正确——Dac/串口看相位差
- 调试kp、ki参数时，先给的较小一点，电压环带宽低，PI控制器调节较慢
- 调试时，确实要采集输入电压，做前馈，才能有较好的负载调整率；带载输出后，输入电压会掉

# 第四轮修改（DQ电压电流双环）

### 一、控制结构

这套 DQ 电压电流双环属于离网逆变器常见的级联控制：

```
输出电压参考 vd*/vq*
        ↓
    电压外环PI
        ↓
电流参考 id*/iq*
        ↓
    电流内环PI
        ↓
电压前馈 + dq解耦
        ↓
逆变器电压指令 ud*/uq*
        ↓
除以直流母线电压Vdc
        ↓
反Park + SVPWM + TIM8
```

“电压环”负责输出电压稳态精度，“电流环”负责快速控制滤波电感电流、改善负载突变响应。

#### 1. 电压外环

误差分别为：

\[ e_{vd}=v_d^*-v_d \]

\(e_{vq}=v_q^*-v_q=0-v_q\)

两个PI的输出分别作为电流参考：

\[ i_d^*=PI_{vd}(v_d^*-v_d) \]

\(i_q^*=PI_{vq}(0-v_q)\)

这是理解双环的关键：电压PI输出的单位应该解释为安培，而不是伏特。

#### 2.电流内环

误差：

\[ e_{id}=i_d^*-i_d \]

\[ e_{iq}=i_q^*-i_q \]

PI输出：

\[ \Delta u_d=PI_{id}(i_d^*-i_d) \]

\[ \Delta u_q=PI_{iq}(i_q^*-i_q) \]

这里电流PI输出应当解释为“需要增加的逆变器电压校正量”，单位为伏特。

#### 3.输出电压前馈

电流PI输出没有直接送给SVPWM，而是加入测得的输出电压：

```
s_v_inv.d = s_id_pi.Out + s_v.d + ...;
s_v_inv.q = s_iq_pi.Out + s_v.q + ...;
```

也就是：

\[ u_{inv,d}^*=\Delta u_d+v_d+\text{解耦项} \]\[ u_{inv,q}^*=\Delta u_q+v_q+\text{解耦项} \]

为什么要加入 `vd/vq`？

滤波电感两端电压决定电流变化：

\[ L\frac{di}{dt}=u_{inv}-u_o \]

如果希望电感上产生某个控制电压，逆变器必须首先抵消已经存在的输出端电压。因此：

```
逆变器电压指令 = 输出电压前馈 + 电流调节所需压差
```

没有这个前馈时，电流PI需要自己积累出整个输出电压，动态响应会明显变慢。

#### 4.dq交叉耦合补偿

代码定义滤波电感：

```
#define DQ_FILTER_L_H 0.001f
```

即：

\[ L=1mH \]

角频率：

\[ \omega=2\pi f \]

即：

\[ u_{inv,d}^* = \Delta u_d+v_d-\omega Li_q \]

\(u_{inv,q}^* = \Delta u_q+v_q+\omega Li_d\)

这里的符号与当前代码采用的 Park 变换一致：d轴使用 `-ωLiq`，q轴使用 `+ωLid`。这些项是dq坐标变换带来的交叉耦合补偿。

如果没有补偿：

- d轴电流变化会影响q轴；
- q轴电流变化会影响d轴；
- 两个电流PI不再近似独立；
- 调参难度增加。

#### 5. 第二阶段控制模式

第二阶段不修改20 kHz边沿对齐PWM和TIM8 CH4中点同步采样。控制模式只允许在编译期通过`DQ_CONTROL_MODE`选择：

```c
#define DQ_OPEN_LOOP                 0U
#define DQ_VOLTAGE_LOOP              1U
#define DQ_VOLTAGE_CURRENT_LOOP      2U

#ifndef DQ_CONTROL_MODE
#define DQ_CONTROL_MODE              DQ_VOLTAGE_LOOP
#endif
```

- `DQ_OPEN_LOOP`：`ud=vd_ref`、`uq=0`，仍保留软启动、SVPWM、过流和故障处理。
- `DQ_VOLTAGE_LOOP`：保留原来的参考前馈加DQ电压PI，默认仍使用该模式。
- `DQ_VOLTAGE_CURRENT_LOOP`：电压外环每4个TIM8周期运行一次，即5 kHz；电流内环每个TIM8周期运行，即20 kHz。
- 不提供运行时模式切换，也不增加手动电流模式。

双环新增四个PI的默认增益均为0，仍采用工程原有离散形式：

```c
integral_candidate = integral + ki * kp * error;
output = kp * error + integral_candidate;
```

因此这里的`ki`是当前工程使用的离散调节系数，不按连续域`1/s`参数解释。单电压环参数保持`kp=0.035`、`ki=0.006`、`vref=30.6 Vrms`。

#### 6. 电流反馈和采样角补偿

电流反馈由实测`Iu/Iw`恢复第三相并完成Clarke/Park变换：

```c
iv = -(iu + iw);
i_alpha = iu;
i_beta = (iv - iw) * DQ_INV_SQRT_THREE;
id = i_alpha * cos_sample + i_beta * sin_sample;
iq = -i_alpha * sin_sample + i_beta * cos_sample;
```

仅在双环模式中，使用`theta_sample = theta_command - 0.5 * theta_step`补偿ADC中点采样到下一次TIM8更新中断之间约25 us的固定延迟。代码用半步固定旋转常数和已经计算的`sin(theta_command)`、`cos(theta_command)`得到采样角，不增加第二组`sinf/cosf`。

#### 7. 级联限幅和抗积分饱和

- 外环输出的`id_ref/iq_ref`整体限制为3.5 A，保持矢量方向。
- 电流PI输出的`delta_ud/delta_uq`整体限制为10 V。
- 最终`ud/uq`整体限制为`0.9 * vdc / sqrt(3)`。当前应用层传入固定`vdc=60 V`，限制值为31.1769 V。
- 占空比仍按`duty = 0.5 + phase_command / vdc`计算并限制在5%至95%。
- 任一级发生矢量饱和时冻结该级两个轴的积分；只有候选积分能减小饱和矢量幅值时才允许积分退饱和。
- 最终电压矢量饱和后，下一次5 kHz外环更新也不允许继续向饱和方向积分。
- 启动、软停止完成、过流故障、清除故障和重新启动都会复位四个双环积分器、外环分频器、参考和饱和标志。

#### 8. 第二阶段UART接口

保留`vp`、`vi`、`vref`、`vstat`原有语义，新增：

```text
ovp=<value>     Outer voltage-loop Kp, range 0.0 to 2.0
ovi=<value>     Outer voltage-loop Ki, range 0.0 to 2.0
icp=<value>     Inner current-loop Kp, range 0.0 to 20.0
ici=<value>     Inner current-loop Ki, range 0.0 to 2.0
dqstat=1        Print cascaded-loop status
```

四个新增增益只能在`wave8=0`时修改，运行中返回`busy`，越界或非法数值返回`invalid`。`dqstat`在主循环中输出模式、母线电压、DQ电压、电流参考和反馈、DQ电压指令、四个增益及限幅标志，不在20 kHz中断内打印。

### 二、DQ双环实施前置任务：TIM8中点同步ADC采样

#### 1. 为什么必须同步采样

原来的ADC配置为软件启动、连续自由运行。ADC转换时刻与PWM载波没有固定关系，TIM8控制中断读取DMA时可能遇到以下情况：

- 每个PWM周期读取到的样本相位不同；
- ADC采样点随机靠近功率开关沿，开关噪声不固定；
- ADC1和ADC2的数据可能来自不同的转换时刻；
- DMA可能正在更新缓冲区，反馈值容易出现不规律抖动。

同步采样的核心不是提高ADC速度，而是让ADC每次都在PWM周期的同一位置开始转换，使采样相位固定、控制延迟可计算。

#### 2. TIM8中点触发原理

TIM8时钟为168 MHz，计数范围为0到8399，因此PWM周期为：

\[
T_{PWM}=\frac{8400}{168\,MHz}=50\,us
\]

对应PWM频率为20 kHz。TIM8 CH4仅作为内部触发通道，配置为PWM2模式，比较值为4200：

```text
CNT=0                 CNT=4200                 CNT=8399
  |----------------------|-------------------------|
  0 us                  25 us                    50 us
                          ^
                          TIM8 CH4 OC4REF rising edge
```

当计数器到达4200时，CH4的内部`OC4REF`信号产生上升沿。TIM8将`OC4REF`选为TRGO输出，TRGO再同时触发ADC1和ADC2。

完整触发链为：

```text
TIM8 counter reaches 4200
            -> CH4 OC4REF rising edge
            -> TIM8 TRGO
            -> ADC1 and ADC2 start one scan sequence
```

CH4不连接到外部引脚，因此PC9继续保持原有GPIO功能。

#### 3. ADC1和ADC2的采样顺序

两个ADC由同一个TIM8 TRGO同时启动，各自完成两个Rank：

```text
ADC1 Rank 1: Iw
ADC1 Rank 2: Uvw

ADC2 Rank 1: Iu
ADC2 Rank 2: Uuv
```

因此`Iw/Iu`基本同时采样，`Uvw/Uuv`基本同时采样。Rank 1和Rank 2之间仍相差一个ADC通道转换时间。

ADC时钟约为21 MHz，单通道使用15个采样周期和约12个转换周期：

\[
T_{channel}\approx\frac{15+12}{21\,MHz}\approx1.29\,us
\]

两个Rank约需2.58 us。ADC在PWM中点开始转换，并在下一个TIM8更新中断前约22 us完成DMA写入：

```text
0 us          25 us       27.6 us                     50 us
PWM start     ADC trigger ADC and DMA complete        TIM8 ISR
|----------------|------------|---------------------------|
```

TIM8更新中断读取的是上一个PWM中点已经完成的数据，此时DMA不在写这组缓冲区，因此读取窗口稳定。

#### 4. 连续DMA与连续ADC的区别

ADC配置为：

```text
ContinuousConvMode = DISABLE
ExternalTrigConv = TIM8 TRGO
ExternalTrigConvEdge = rising edge
DMAContinuousRequests = ENABLE
```

含义是：每收到一次TIM8 TRGO，ADC只执行一次双通道扫描；下一次扫描必须等待下一个TRGO。`DMAContinuousRequests`只表示后续每次外部触发仍继续使用DMA，不表示ADC自由运行。

DMA将结果循环写入：

```text
adc_val_buffer[0]   = Iw
adc_val_buffer[1]   = Uvw
adc_val_buffer_2[0] = Iu
adc_val_buffer_2[1] = Uuv
```

控制计算统一由20 kHz的TIM8更新中断执行，不需要DMA半传输和传输完成回调，因此关闭DMA HT和TC中断，避免两个ADC额外产生大量无用中断。

#### 5. 初始化顺序

改为外部触发后，TIM8不运行就没有TRGO，ADC也不会产生数据。因此初始化顺序必须为：

```text
Initialize filters
    -> Start ADC1 and ADC2 DMA
    -> Start TIM8 CH4 internal trigger
    -> Start TIM8 update interrupt
    -> Wait 2 ms
    -> Perform 256-sample offset calibration
    -> Start TIM13
```

TIM8计数器在`wave8=0`、软停止和故障锁存状态下仍继续运行，只通过MOE关闭三相功率PWM输出。这样停机状态下ADC仍能同步采样并进行偏置跟踪。

#### 6. 固定采样延迟

ADC在PWM中点采样，控制算法在下一个周期边界使用该样本，因此固定延迟约为25 us。对于50 Hz基波，对应相位为：

\[
50\,Hz\times25\,us\times360^\circ=0.45^\circ
\]

该延迟很小且可预测。第一阶段不修改现有`theta`和Park变换，后续接入电流双环时再统一进行采样角度和控制延迟补偿。

#### 7. 当前方案的限制

当前TIM8使用边沿对齐PWM。这里的“中点同步”表示采样时刻固定在载波周期中点，不等于该位置一定没有功率开关动作。

当某相占空比接近50%时，该相CCR也可能接近4200，PWM比较沿可能与ADC触发点重合，电流采样仍可能捕获到开关尖峰。因此当前阶段完成的是：

```text
Random asynchronous sampling -> Fixed carrier-phase sampling
```

上板时必须重点观察50%占空比附近的电流反馈噪声。如果尖峰明显，后续应评估调整CH4位置、根据占空比选择安全窗口，或改为中心对齐PWM并在载波峰值/谷值采样。

#### 8. 实际实现记录（2026-07-27）

- TIM8 CH4配置为内部PWM2，`CCR4=4200`，TRGO选择`OC4REF`；PC9仍为GPIO。
- ADC1和ADC2关闭连续转换，改为TIM8 TRGO上升沿启动双通道扫描。
- ADC DMA启动失败会进入`Error_Handler()`，启动后关闭HT和TC中断。
- TIM8在偏置校准前启动，并等待2 ms确保DMA缓冲区已有有效数据。
- PI计算方式保持不变；当前单电压环默认值为`kp=0.035`、`ki=0.006`、`vref=30.6 Vrms`。
- 新增源代码和代码注释只使用英文ASCII，不转换旧编码源文件。
- Keil ARMCC 5.06u7全量构建结果：`0 Error(s), 0 Warning(s)`。

### 三、DQ双环第二阶段实际实现记录（2026-07-27）

- 新增纯ASCII模块`Component/Dq_control.c/.h`，实现四个PI状态、5 kHz外环分频、20 kHz内环、DQ解耦、三级限幅、条件积分抗饱和、复位和状态快照。
- `Spwm_app.c`新增电流Clarke/Park和三种编译模式；默认模式仍为`DQ_VOLTAGE_LOOP`，单电压环PWM指令算法保持不变。
- 双环模式使用25 us采样延迟的半步角补偿，解耦符号为d轴`-omega*L*iq`、q轴`+omega*L*id`。
- 应用层当前固定传入`vdc=60.0f`，没有新增ADC通道；后续真实母线采样只需替换该输入。
- 新增`ovp`、`ovi`、`icp`、`ici`和`dqstat`串口命令，增益默认均为0且只允许停机修改。
- 第一阶段TIM8 CH4、ADC外部触发和DMA配置未修改，PWM继续使用20 kHz边沿对齐模式。
- Keil ARMCC 5.06u7分别全量构建三种`DQ_CONTROL_MODE`，结果均为`0 Error(s), 0 Warning(s)`；最终默认值已恢复为`DQ_VOLTAGE_LOOP`。
- 主机单元检查已验证新增增益默认值、5 kHz外环分频、3.5 A/10 V/31.1769 V矢量限幅、饱和积分冻结和复位清零。

### 四、上板调试

1. 编译`DQ_VOLTAGE_LOOP`，保持当前参数，使用`dqstat`验证电流DQ极性。
2. 编译`DQ_OPEN_LOOP`，设置`vref=3`，确认软启动、50 Hz相序、SVPWM和保护行为未改变。
3. 编译`DQ_VOLTAGE_CURRENT_LOOP`，保持四个新PI增益为0，确认不会产生非预期电压指令。
4. 停机设置`vref=3`、`ovi=0`、`ici=0`，从很小的`ovp`和`icp`开始，先建立受3.5 A限制的弱闭环。
5. 先逐步增加`icp`，再加入很小的`ici`；确认`id/iq`跟踪稳定后再调`ovp/ovi`。
6. 最后恢复`vref=30.6`并接入9 ohm纯电阻负载，检查相电流峰值约2.9 A、`iq`接近0且不触发5 A保护。
7. 测量TIM8 ISR执行时间，必须小于50 us，目标不超过25 us。
8. 验证软停止、过流锁存、清故障和再次启动后不存在积分残留或电流参考突跳。
